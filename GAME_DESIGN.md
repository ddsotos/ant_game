# アリ進化ゲーム設計書（v0.3 Daybreak型コア）

## 0. 文書の位置づけ

この文書は、現在のゲームコンセプトの唯一の正本である。旧v0.1/v0.2のルール、数値、終了条件は、本書と矛盾する限り採用しない。過去の実験結果はログ・アーカイブに残すが、現在のルールを決める根拠にはしない。

本版は、承認済みの再設計を文書化した初期試作仕様である。確定事項と、実装・シミュレーションで比較する候補を分けて記載する。

## 1. プロジェクト目標

プレイヤーは実在するアリの適応を題材に、一つのアリ種を進化させる。中心となる判断は次の三つである。

- いま繁栄点を得るために起動するか、将来の進化ルートを残すか。
- どの列にカードを置き、古い進化基盤を残すか押し出すか。
- 災害を完全に防ぐか、ラウンド限定シールドで被害を抑え、ダメージを受けながら得点するか。

目標は万能な種を作ることではない。

> **進化の土台が十分になったと判断した時点で準備をやめ、残り時間を繁栄へ変換する。**

UIの完成度よりも、短時間で繰り返し遊べ、繁栄・柔軟性・専門化・環境準備の間に状況依存のジレンマが生じることを優先する。

## 2. 初期版で確定しているルール

以下は、次の実装・テストで変更せずに扱う事項である。

### 2.1 ゲームの長さ

- ゲームは全5ラウンド。
- ゲーム開始時、環境カードを1枚公開する。
- 環境の段階はラウンドごとに **I / I / II / III / IV** と進む。
- 5ラウンド終了時、または途中絶滅時にゲームを終了する。
- 旧設計の12〜18ラウンドの大絶滅時計や、5ラウンドを超える時計は使用しない。

| ラウンド | 環境段階 | 位置づけ |
|---:|:---:|---|
| 1 | I | 環境と対応候補を公開。軽い圧力 |
| 2 | I | 準備の猶予。軽い圧力 |
| 3 | II | 脅威が顕在化。中程度の圧力 |
| 4 | III | 災害のピーク |
| 5 | IV | 余波と最後の繁栄判断 |

各環境カードは段階ごとの威力を持つ。Stage IVを自動的に無害とはしない。

### 2.2 サイズ

サイズは4段階である。

1. Small
2. Medium
3. Large
4. Giant

毎ラウンド、現在サイズから1段階上げる、1段階下げる、または維持する。サイズの役割は、主に繁栄倍率と新規カードを手札へ残せる枚数の差で表す。サイズ変更だけで自動的にカードやタグを失わせない。

ゲーム開始時のサイズはSmallとする。これにより、5ラウンドの中で準備を続けるか、早く繁栄へ移るかを最初のラウンドから判断させる。

### 2.3 カード公開と保持

- すべてのサイズで、毎ラウンド通常カードを6枚公開する。
- サイズによって、6枚から手札へ保持できる枚数だけが変わる。
- 手札の初期上限は8枚を基準とする。
- 保持しなかったカードは捨て札に置く。
- 極端適応を取得する場合も、通常カードと同じ保持枠を使う。

保持曲線は比較対象であり、最終値は未決定である。最初に比較する曲線は次の二つとする。

| 曲線 | Small | Medium | Large | Giant |
|---|---:|---:|---:|---:|
| Aggressive | 4 | 3 | 2 | 1 |
| Soft | 4 | 3 | 3 | 2 |

### 2.4 進化列（Daybreak型）

- 種は3列または4列の進化列を持つ。
- 手札のカードは任意の列の先頭へ置ける。
- 古いカードは新しいカードの下に残り、印刷されたタグだけを供給する。
- 列の先頭は、その列で現在見えている一番上のカードである。
- 先頭以外の下層カードは、タグ以外の効果を持たない。
- 新しいカードが容量を超えた場合、配置した同じ列の最古カードを押し出す。
- プレイヤーは任意の古いカードを選んで捨てない。
- 災害、サイズ変更、ラウンド経過による自動削除は行わない。

列数、容量方式、容量値は比較実験として残す。初期比較の基準は、3列なら各列5枚、4列なら各列4枚である。全体容量方式も同じ総容量を目安に比較する（詳細は「未決定事項」参照）。

### 2.5 起動効果

- 通常カードの90%以上は `ACTION`（起動効果）である。
- 起動できるのは、ラウンド中その列の先頭にあるカードだけ。
- 同じ物理カードは、1ラウンドに1回だけ起動できる。
- 起動時、カード自身を含む同じ列の全カードのタグを条件判定に使う。
- 起動したカードを別のカードで覆い、その新しい先頭を同じラウンドに起動してよい。
- これにより、旧先頭の起動 → 配置・被覆 → 新先頭の起動、という連鎖を許可する。
- 覆われたカードはタグを残すが、起動、移動、先頭への昇格はできない。
- ラウンド内のカード起動回数に全体上限は設けない。カード個別の1回制限と手札・列容量が制約になる。

通常カードの例外として、スターター、環境カード、極少数の配置時効果または常時効果を許可する。ただし例外を増やす前に、ACTION中心の構造でジレンマが生じるかを検証する。

### 2.6 支援カード

支援配置は、手札の通常カードを列の下層へ差し込み、印刷されたタグだけを追加するDaybreak型の使い方である。専用の支援カード種別は作らない。

- 支援として置いたカードはタグだけを残す。
- 支援として置いたカードの効果は起動できない。
- 支援配置は列容量を消費する。
- 配置により現在の先頭が押し出される場合、その配置はできない。

同じカードを先頭へ置いて効果を使うか、支援として効果を捨ててタグを残すかをプレイヤーが選ぶ。一度支援配置したカードは移動・起動・先頭への昇格ができない。

### 2.7 起動効果の範囲

起動効果には、繁栄点を稼ぐものだけでなく、次を含める。

- 現ラウンドの繁栄点を得る。
- 現ラウンドの災害ダメージに対するシールドを得る。
- カードを引く、または保持枠を一時的に補助する。
- タグや別カードの効果を現ラウンドだけ強化する。
- 環境固有の対応ルートを成立させる。

したがって、起動するかどうかは「得点を得るか」だけでなく、「今回の災害に備えるか」も含む判断である。

### 2.8 ラウンド限定シールド

- シールドは環境解決時の災害ダメージから差し引く。
- 適用可能なシールドをすべて加算し、軽減後のダメージは0未満にならない。
- 未使用シールドはラウンド終了時に消滅し、次ラウンドへ持ち越さない。
- シールドはダメージだけを軽減する。繁栄抑制など別の効果を防ぐ場合はカードに明記する。
- 原則として、シールドが対応できる環境または圧力をカードに明記する。
- 救護・集団防御など、実在の生物学的根拠がある汎用的な効果のみ複数環境への対応を許可する。

同じカードであっても、繁栄を起動するか、シールドを起動するか、起動後に覆って両方の連鎖を狙うかが状況で変わることを目標とする。

### 2.9 極端適応

- 極端適応は独立デッキ、市場、ランダム山札にしない。
- 各環境カードに1〜2種類を付記し、Stage Iから名称、条件、解禁段階を公開する。
- 環境はカードを自動で与えず、進化の機会だけを提示する。
- 条件を満たして取得する場合、通常カード1枚の代わりに保持枠を使って手札へ入れる。
- 取得後は通常カードと同じように列へ配置し、先頭なら起動できる。
- 取得済みカードには、どの環境に付記されていたかを内部メタデータとして記録する。
- 未取得の候補はStage IV終了時に消える。取得済みカードは手札・列に残る。
- 極端適応は防御だけでなく、繁栄または進化連鎖上の価値も持つようにする。

### 2.10 実在アリの題材

通常カード、スターター、極端適応は、可能な限り実在するアリの形態、生理、行動、生態を題材にする。正式採用カードは、少なくとも次の内部メタデータを持つ。

- `source_taxon`：根拠となる種・属・グループ
- `biology_basis`：カード効果に対応する実在の適応
- `biology_source`：確認に用いた論文・資料・URL
- `root_tags`：進化上の根タグ（通常1〜2個）
- `design_role`：Foundation / Payoff / Bridge / Extreme

種レベルで裏付けられない場合は、無理に種名を付けず属・グループ単位で表記する。`Flood Resistance +2`のような一般能力名は、内部テスト用の仮ラベル以外では使わない。

初期候補には、ヒアリの筏形成、カメアリの滑空・頭部栓、サシハリアリの毒、トラップジョーアリの顎、ハキリアリの菌栽培、ミツツボアリの貯蔵個体、マタベレアリの負傷個体救護などを含める。正式採用前に各根拠を確認する。

## 3. タグとカード役割

### 3.1 根タグ

タグは環境そのものではなく、進化上の根・仕組みを表す。初期タグは次の7種類に固定する。

| タグ | 意味 |
|---|---|
| Morphology | 顎、頭部、体形、装甲、特殊毛などの形態 |
| Chemistry | 毒、抗菌分泌、フェロモン、化学信号 |
| Cooperation | 筏、救護、集団建設、協同輸送などの特殊な集団能力 |
| Caste | 兵隊、超大型個体、貯蔵個体などの分業形態 |
| Nesting | 巣構造、入口、防衛、菌室、廃棄室など |
| Movement | 滑空、跳躍、遊牧、探索、長距離移動など |
| Resource Ecology | 貯蔵、菌栽培、アブラムシ、採餌、植物との関係 |

環境名（洪水、暑熱、病害、捕食など）を恒久的なプレイヤータグにしない。カードの環境効果と根タグは別に扱う。

### 3.2 カード役割

初期カードプールは次の役割を試す。比率は目安であり、確定値ではない。

- Foundation：条件が軽く、進化の根を作るカード。40〜50%。
- Payoff：既存タグが揃うほど強くなるカード。35〜45%。
- Bridge：離れたタグ群を接続するカード。
- Extreme：環境付記の極端適応、および高条件のカード。10〜20%の範囲を目安。

通常カードの大半はACTIONだが、すべてを条件カードにしない。典型的な手札に「1〜3ラウンド後に強くなるので保持したいカード」が存在することを重視する。

### 3.3 スターター

開始時は、すでに特殊種になっていない普通のアリを表す。初期候補は次の通り。

- `TRAIL PHEROMONE`（Chemistry / Movement）
- `EARTHWORK NEST`（Nesting）
- `COLLECTIVE FORAGING`（Cooperation / Resource Ecology）

3枚を別々の列に置く。4列構成を試す場合も第4列は空で開始し、スターター枚数による有利不利を混ぜない。

## 4. 5ラウンドの進行

各ラウンドは環境段階を先に確認してから、プレイヤーが準備と繁栄の比率を決める。

1. 現在の環境段階、予想ダメージ、適用可能なシールド、公開中の極端適応を確認する。
2. Small / Medium / Large / Giantからサイズを選ぶ（維持可）。
3. 通常カードを6枚公開する。
4. 現サイズの保持枚数まで通常候補を手札へ残す。極端適応を取得する場合はここで通常候補1枚と置き換える。
5. 手札から任意の枚数を任意順で配置・支援配置し、列の先頭を起動する。各物理カードはラウンド1回まで。
6. 環境効果と災害ダメージを解決する。起動済みシールドを差し引く。
7. 生存していれば、そのラウンドの繁栄点をサイズ倍率込みで得る。
8. 一時効果、ラウンド限定シールド、起動済み状態を消去する。

起動・配置の順序は、同じラウンド内で「起動 → 覆う → 新先頭を起動」が可能であることを満たす。カード公開・保持の詳細なタイミングにより結果が変わる場合は、実装テストで記録し、勝手に別の恒久ルールを追加しない。

5ラウンド目のStage IVを解決し、繁栄点を得た後にゲームを終了する。5ラウンド後の大絶滅フェイズ、勝利ボーナス、追加ラウンドは初期版に入れない。

## 5. 繁栄、ダメージ、終了

### 5.1 繁栄点

カードの起動効果で得る繁栄を、そのラウンドのサイズ倍率で増幅する。初期比較用の倍率候補は次の通りだが、バランス検証で変更し得る。

| サイズ | 倍率候補 |
|---|---:|
| Small | ×0 |
| Medium | ×1 |
| Large | ×2 |
| Giant | ×3 |

Smallは将来のカードを保持しやすく、Giantは保持枠を失う代わりに、整えた繁栄エンジンを大きく得点化する。サイズそのものから無条件の固定点は得ない。

### 5.2 災害ダメージ

環境カードが段階ごとの基礎ダメージを定義し、カードの対応効果やシールドで軽減する。ダメージ計算は次の形を基準にする。

> 軽減後ダメージ = max(0, 環境基礎ダメージ − 対応効果 − ラウンド限定シールド)

具体的な段階威力、対応効果、途中絶滅の閾値、無対策時のペナルティ曲線は未決定である。初期ヘッドレス実装では、単純な累積ダメージと閾値を基準モデルにし、別モデルと比較する。これを「確定済みの最終バランス」と解釈しない。

### 5.3 途中絶滅

累積ダメージがテスト中の閾値に達した場合、その種は途中絶滅し、その後のラウンドで行動・得点しない。すでに得た繁栄点を失うかどうかは、最新方針では「原則保持」を初期基準とする。逃げ切りが支配戦略になった場合は、専用Botの結果を根拠に再検討する。

## 6. 確定事項と未決定事項

### 6.1 確定事項

- 初期版は5ラウンド。
- 環境段階はI / I / II / III / IV。
- 全サイズで通常カード6枚を公開し、保持枚数をサイズ差にする。
- サイズはSmall / Medium / Large / Giant。
- 開始サイズはSmall。
- Daybreak型の複数列、下層カードのタグ供給、先頭起動を使う。
- 通常カードの90%以上はACTION。
- 同一物理カードはラウンド1回だけ起動。
- 起動 → 覆う → 新先頭起動の連鎖を許可。
- 下層カードと支援カードはタグだけを残す。
- 起動効果は繁栄点とラウンド限定シールドの両方を含む。
- シールドはそのラウンドの災害ダメージだけを軽減し、持ち越さない。
- 極端適応は環境カード付記で公開し、保持枠を使って手札へ入れ、通常配置する。
- カードは実在アリを題材にし、根拠メタデータを持つ。
- コアロジックとUIを分離し、ソロCLIで検証する。
- 各デザイン反復は、シミュレーション・fresh Lunaレビュー・Solの最大2変更提案の後、ユーザー承認を得て次へ進む。

### 6.2 未決定・比較する事項

- 3列と4列のどちらが、配置の痛みと専門化を最もよく生むか。
- 列ごとの容量か、全体容量か。
- 初期容量（候補：3列×5、4列×4、同程度の総容量）。
- 保持曲線Aggressive（4/3/2/1）とSoft（4/3/3/2）のどちらが適切か。
- 手札上限8の妥当性。
- サイズの繁栄倍率（初期候補Small 0 / Medium 1 / Large 2 / Giant 3）。
- 環境段階ごとの威力と、対応効果の量。
- 途中絶滅の閾値と、ダメージを受けたまま得点する頻度。
- 累積ダメージ、繁栄抑制併用、非線形（cliff）などの環境失敗モデル。
- Extremeカードの枚数、条件、シールドと繁栄の配分。
- 3枚のスターターの最終的な効果値。

未決定事項を埋めるために新しい資源トラックや複雑な例外を追加しない。まず列、手札、サイズ、起動、シールドだけで中心ジレンマが成立するかを確認する。

## 7. 実装方針

- ゲームロジックは決定論的なseedを受け取れるヘッドレスエンジンにする。
- ソロCLIで、ラウンド、環境段階、サイズ、手札、各列のカード・タグ、起動可能な先頭、繁栄、予想ダメージ、シールドを表示する。
- 環境・カード・タグはデータ駆動で定義する。
- 画面装飾、アニメーション、サウンド、複雑なメニューは作らない。
- 同じルールをBotシミュレーションとCLIが共有する。

カードデータには、通常カードか支援カードか、効果種別、タグ、起動条件、起動済み状態、繁栄値、シールド対象、実在アリ根拠を保持する。環境データには、段階威力、公開する極端適応、その条件、対応する圧力を保持する。

## 8. シミュレーションとレビュー

次の単純Botを用意する。

- prosperity-first / Large・Giant優先
- flexibility-first / Small優先
- reactive / 予想ダメージとシールドを優先
- specialization / 一つのタグ群を集中
- generalist / 複数タグを均等化
- random baseline

記録する統計は次の通り。

- 最終繁栄点、途中絶滅率、絶滅ラウンド
- ラウンドごとのサイズ、サイズ変更回数
- 保持、配置、起動、支援、押し出しの回数
- 取得・破棄したカード、極端適応の取得・見送り
- シールド取得量、使用量、余剰量
- 災害ダメージ、軽減後ダメージ、繁栄との交換
- タグの幅、列ごとの深さ、起動連鎖長
- 戦略別の勝率と代表的な意思決定ログ

統計だけで「楽しい」「有望」と判定しない。代表ゲームを読み、次を確認する。

- 少なくとも二つの選択に、状況依存の利点があるか。
- 環境段階の進行で最善のサイズやタグが変わるか。
- 良いカードを保持する理由があるか。
- 配置によるタグ喪失を後悔する場面があるか。
- Giantでもカードコンボの選択が残るか。
- ダメージを受けて得点する判断が合理的になるか。

各反復では、fresh Lunaのbalance、dilemma、adversarial、simplicity、ant-traitの観点を分けてレビューする。Solは多数決ではなく、本文の目標とテスト結果を基準に採否を決める。採用する変更は一度に最大2件とし、ユーザー承認なしに次の反復へ進めない。

## 9. テスト基準

最低限、次を自動テストする。

- ゲームが必ず5ラウンド以内に終了する。
- 同一seedと同一行動で結果が再現する。
- 環境段階がI / I / II / III / IVになる。
- 全サイズで6枚が公開され、保持枚数だけがサイズ曲線に従う。
- 手札上限を超えて保持できない。
- 同じカードをラウンド内に再起動できない。
- 起動後に覆い、新しい先頭を同じラウンドに起動できる。
- 下層カードと支援カードはタグを供給するが効果を起動できない。
- 列容量超過時に、配置列の最古カードだけが押し出される。
- シールドが適用対象の災害ダメージだけを軽減する。
- シールドがラウンド終了時に消える。
- 環境付記の極端適応だけが公開・取得され、取得後は通常カードとして扱われる。
- 正式カードに実在アリの根拠メタデータがある。

## 10. 反復の停止基準

少なくとも3回連続の設計反復で、次のいずれも明白な支配戦略にならないことを確認する。

- Smallに留まり続けるだけの戦略。
- 最終ラウンドにGiant化するだけの戦略。
- 環境を無視して繁栄だけを起動する戦略。
- 特定の一枚を引けたかだけで生存が決まる戦略。
- どの列にも同じように置けるタグの万能化。
- シールドを積むだけ、またはダメージを受けるだけの固定解。

この条件を満たす前にUIを磨かない。大幅な再設計が必要だと分かった場合は、ルールを追加し続けず、どの構造が失敗したかをログに残して親エージェントへ報告する。

## 11. 初期版の一文

> **実在アリの進化を列に積み、将来の可能性を保持するか押し出すかを選び、災害への一時的な備えと繁栄への加速を5ラウンドで競う。**
