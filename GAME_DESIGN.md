# アリ進化ゲーム設計書 — v0.6 五根タグ／操作支援版

## 0. この文書の位置づけ

本書は現在のゲーム概念と実装判断の唯一の正本である。v0.4以前の「災害カードがダイス対象も兼ねる」構造は現行ルールではない。過去の試行は `PLAYTEST_LOG.md` と `docs/archive/` に残す。

v0.6は、人間がブラウザで遊び、判断の質を評価するための5ラウンド試作である。バランス完成版でも、NPCによる `promising` 判定済みの版でもない。

## 1. 中心となる判断

実在するアリの適応を3本の進化列へ積み、次を同時に考える。

- 大型化して今ラウンドの繁栄を増やすか、小型を保って多くのカードを確保するか。
- 繁栄効果を起動するか、現在の問題を防ぐシールドを起動するか。
- 強い先頭カードを残すか、覆ってタグに変え、将来の最適化を進めるか。
- 条件なしの即応カードを使うか、準備が必要だが強いカードへ投資するか。

「環境変化」と「問題」は別物として扱う。

- 環境変化：5ラウンドを通じて予報できる、生息環境の長期的変化。最適化目標を示す。
- 問題：どの環境でも毎ラウンド起こり得る襲撃・菌害・巣の損傷。1d4と一時シールドで処理する。

## 2. ゲーム概要

- 全5ラウンド。
- 5枚の環境変化カードをseedで並べ替え、開始時に順番をすべて公開する。
- 各ラウンド開始時、3つの常設問題それぞれに1d4を振り、行動前に公開する。
- 5ラウンド終了時の繁栄点が最終得点。絶滅はない。
- 同じseedと同じ操作なら同じ結果を再現する。

## 3. サイズとカード保持

開始サイズはSmall。ラウンド開始時に現在サイズから1段階だけ上下、または維持できる。

| サイズ | 繁栄倍率 | 公開6枚から保持できる枚数 |
|---|---:|---:|
| Small | 0 | 4 |
| Medium | 1 | 3 |
| Large | 2 | 2 |
| Giant | 3 | 1 |

毎ラウンド、全サイズで通常カードを6枚公開する。保持しなかったカードは捨て札へ送る。手札上限は8枚。実際に保持できる枚数は、サイズ上限と手札の空き枠の小さい方である。

## 4. 進化列

- 3列、各列の容量は5枚。
- 手札カードを任意の列の先頭へ置ける。
- 覆われたカードはタグを残すが、効果は起動できない。
- 列の先頭カードだけが、その物理カードにつき1ラウンド1回起動できる。
- 新しい先頭を置いた後、そのカードは同じラウンド中でも起動できる。
- 容量を超えると、その列の最古カードを押し出す。
- プレイヤーが任意に盤面カードを破棄・移動することはできない。

### 4.1 起動条件

先頭カード自身のタグは、そのカード自身の起動条件に数えない。同じ列にあるほかのカードのタグだけを数え、他列は参照しない。

条件なしカードを十分に用意し、初手や引きに左右されず行動できるようにする。特に基本的なシールド+1は原則として条件なしでよい。一方、次は条件を要求する。

- ドロー：単純に強い能力なので少数にし、原則として条件付き。
- 強固なシールド：シールド3以上を目安に、重いタグ条件と組み合わせる。
- 高い繁栄や複合効果：効果量に応じて条件を持たせる。

条件はカード自身と異なるタグを要求してもよいが、実在アリの生態から自然に説明できなければならない。

### 4.2 支援配置

手札カードを列の先頭直下へ支援として差し込める。支援カードはタグだけを供給し、効果を永久に失う。差し込みによって現在の先頭を押し出す配置はできない。

## 5. 根タグ

カードは実際の特徴に応じて1～3個の根タグを持つ。`Cooperation`と`Caste`は、どちらもコロニーの社会組織を表すため`Sociality`へ統合する。同じカードが旧2タグを持っていた場合も社会性を二重計上しない。

| タグ | 意味 | 基調色 | シンボル |
|---|---|---|---|
| Morphology | 顎、体形、毛、装甲 | 青 | 大顎 |
| Chemistry | 毒、抗菌、フェロモン | 橙 | 滴 |
| Sociality | 協同、カースト、分業、救護、集団防衛 | 緑 | 社会集団 |
| Nesting | 巣、入口、地下室 | 紫 | 巣穴 |
| Resource Ecology | 貯蔵、栽培、採餌、資源処理 | 黄 | 葉と種子 |

ブラウザではタグを色とシンボルの両方で識別する。複数タグのカードは各色を帯や面に併用する。既存TCGの枠・記号・意匠は模倣しない。

`Movement`は根タグとして廃止する。移動は多くの場合、形態、化学、社会性、巣作り、資源利用によって生じる結果であり、カードごとに生物学的な中心へ再解釈する。独立した説明力がなければ単純に削除する。正式カードごとの判断は`ANT_RESEARCH_CATALOG.md`に記録する。

## 6. 起動効果

代表的な起動効果は次の通り。

- 基礎繁栄点を得る。実得点は基礎値×現在サイズ倍率。
- 指定された問題に対する、このラウンド限りのシールドを得る。
- 条件を満たした一部のカードだけが、カードを1枚引く。

貯蔵は将来の選択肢を確保する性質として、条件付きドローで表現する。貯蔵を乾燥シールドにはしない。未使用シールドはラウンド終了時に消える。

スターター3枚は開始時から無条件で起動できる。スターターにドローは置かず、小さな繁栄または基本シールドを与える。

## 7. 常設問題とシールド

環境変化カードの種類とは無関係に、毎ラウンド次の3問題へそれぞれ1d4を振る。

| 問題ID | 日本語名 | 含むもの |
|---|---|---|
| `raid` | 襲撃 | 捕食者、他コロニー、軍隊アリ、戦闘後の救護 |
| `fungal` | 菌害 | 胞子汚染、傷口感染、菌園病害 |
| `nest_damage` | 巣の損傷 | 局地的浸水、土砂、巣口閉塞、構造破損 |

問題ごとに次を計算する。

```text
n = max(0, 出目 - 対応するシールド)
減点 = 0（n=0） / 2^n（n>0）
```

3問題の減点を合計する。得点は0未満にならない。3個の指数減点は強い圧力になるため、人間プレイで「ほぼ毎ラウンド0点になる」場合は最優先の調整対象とする。v0.6では各問題を1d4とし、指数式は維持する。

## 8. 環境変化と最適化

環境変化カードには問題分類やダイス値を印刷しない。各カードは名前付きの全盤面タグ条件「最適化」を一つ持つ。3列すべてのカードが持つタグを合算し、ラウンド精算時に判定する。

条件未達なら絶滅ではなく、その時点の繁栄点を切り捨て半減する。

| 環境変化 | 最適化 | 必要タグ |
|---|---|---|
| 洪水 | 水没環境への複合適応 | Sociality 3 / Morphology 1 / Nesting 1 / Resource Ecology 1 |
| 砂漠熱波 | 銀毛・熱ショック耐性 | Morphology 3 / Chemistry 2 |
| 長期乾燥 | 地下穀倉 | Nesting 3 / Resource Ecology 2 |
| 居住地不安定化 | 緊急引越し | Sociality 2 / Nesting 2 |
| 目印消失 | 天空コンパス航法 | Resource Ecology 3 |

洪水は、生体いかだと樹冠退避を別カード・別分岐にせず、一つの複合最適化へまとめる。熱ショック応答は通常の熱シールドではなく、砂漠熱波への長期最適化の一部として扱う。胞子汚染と襲撃後の負傷は環境変化から外し、常設問題のフレーバーへ含める。

## 9. 精算順

1. 起動で得た繁栄点を加える。
2. 襲撃・菌害・巣の損傷の順で、各1d4にシールドを適用する。
3. 3問題の指数減点を合計して引き、0で下げ止める。
4. 現在の環境変化の最適化を判定する。
5. 未達なら残点を切り捨て半減する。
6. 一時シールドと起動済み状態を消去し、次ラウンドへ進む。

## 10. コンテンツ方針

- 通常カード、効果名、最適化は可能な限り実在するアリの形態・生理・行動・生態を題材にする。
- 正式採用カードは `source_taxon`、`biology_basis`、`biology_source` を持つ。
- 数値修正だけの一般名ではなく、実際の適応が想像できる固有名を使う。
- 数値の面白さのために生物学的説明を歪めない。
- 現行題材と出典、将来候補は `ANT_RESEARCH_CATALOG.md` に記録する。

## 11. UIと実装

- ロジックはseed指定可能なヘッドレスエンジンとしてUIから分離する。
- 人間向け標準UIは日本語ブラウザ画面。
- 画面上で「5枚の環境変化予報」と「今ラウンドの3問題」を明確に別ブロックへ分ける。
- 各問題の出目、シールド、未防御量、減点を表示する。
- カード自身のタグと、自身を除いた起動条件を区別して表示する。
- 保持カードを選ぶ間も、すでに持っている手札を表示する。
- 通常配置ボタンは、満杯列から最古カードが押し出された後の状態まで計算し、配置直後に起動可能なら緑、必要タグが合計あと1なら黄、それ以外は標準色で示す。支援配置はこの予測色の対象外とする。
- 「一手戻す」は直前に成功した操作の完全なゲーム状態を復元する。ラウンド精算を戻した場合も、乱数状態を含めて復元し、同じ操作をやり直せば同じ結果になる。失敗した操作は戻し履歴へ加えない。
- CLIと自動戦略はデバッグ用に残すが、v0.6の評価は人間プレイを優先する。
- アニメーション、音、装飾的画面遷移は優先しない。

## 12. 検証方針

人間プレイで特に次を確認する。

- 3問題の出目を見て、繁栄と複数シールドのどれを起動するか迷うか。
- 条件なしシールドが最低限の応答を保証しつつ、強シールドの準備が魅力的か。
- 条件付きドローが自動選択にならず、支援や被覆の価値と競合するか。
- 5環境変化を見て、タグ構築の順番を考えたくなるか。
- 社会性への統合で読みやすくなり、カードの個性は残るか。
- 大型化の得点と保持枚数の減少が競合するか。
- 3問題の合計減点が厳しすぎて、繁栄や最適化の判断を無意味にしていないか。

変更は一度に1～3件へ絞り、`PLAYTEST_LOG.md`と`DECISIONS.md`へ理由を残す。

## 13. テスト基準

- 必ず5ラウンドで終了する。
- 同じseedと操作で、環境順、3問題の出目、候補、結果が再現する。
- 5枚の環境変化が開始時から公開され、問題分類を持たない。
- 毎ラウンド3問題すべてに1～4の出目がある。
- 起動条件に先頭カード自身のタグを数えない。
- シールドは対応する問題だけに適用され、ラウンド終了時に消える。
- `2^n`減点、0点下限、最適化未達時の切り捨て半減を指定順で処理する。
- 正式コンテンツとUIに旧`Cooperation`・`Caste`・`Movement`タグ、熱・乾燥シールドが残らない。
- ドローは少数かつすべて条件付き、シールド3以上も条件付きである。
- 正式カードに実在アリ由来の根拠メタデータがある。

## 14. v0.6の一文

> 五つの根タグで公開された環境変化へ長期適応しながら、毎ラウンド1d4で襲う三つの問題に一時シールドで対処し、繁栄との優先順位を選ぶ5ラウンドゲーム。
